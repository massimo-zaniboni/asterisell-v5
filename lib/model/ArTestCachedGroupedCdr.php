<?php

require 'lib/model/om/BaseArTestCachedGroupedCdr.php';


/**
 * Skeleton subclass for representing a row from the 'ar_test_cached_grouped_cdr' table.
 *
 * 
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    lib.model
 */
class ArTestCachedGroupedCdr extends BaseArTestCachedGroupedCdr {

	/**
	 * Initializes internal state of ArTestCachedGroupedCdr object.
	 * @see        parent::__construct()
	 */
	public function __construct()
	{
		// Make sure that parent constructor is always invoked, since that
		// is where any default values for this object are set.
		parent::__construct();
	}

} // ArTestCachedGroupedCdr
