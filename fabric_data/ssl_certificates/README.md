This directory contains the SSL certificates that can be used for the Asterisell instances.

All the files *.crt and *.key inside this directory are put under .gitignore, because
they should be private.
